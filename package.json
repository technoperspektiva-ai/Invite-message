{
  "name": "invite-message",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "setup": "wrangler r2 bucket create love-invite-media || true",
    "deploy": "wrangler r2 bucket create love-invite-media || true && wrangler deploy",
    "deploy:plain": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
